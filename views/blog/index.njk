---
layout: base.njk
title: Writing
permalink: /blog/
---

<div class="prose-muted">
  <header class="mb-12">
    <h1 class="text-4xl md:text-5xl font-semibold tracking-tight mb-4">{{ title }}</h1>
    <p class="text-xl text-muted-light-text-secondary dark:text-muted-text-secondary leading-relaxed">
      Thoughts, ideas, and reflections on technology, design, and life.
    </p>
  </header>

  <div class="grid gap-8 md:gap-12">
    {%- for post in collections.blog | reverse %}
    <article class="blog-post-card">
      <div class="flex flex-col md:flex-row md:items-start gap-6">
        {% if post.data.featured_image %}
        <div class="md:w-1/3 flex-shrink-0">
          <img src="{{ post.data.featured_image }}" alt="{{ post.data.title }}"
               class="w-full h-48 md:h-32 object-cover rounded-lg border border-muted-light-bg-accent dark:border-muted-bg-accent">
        </div>
        {% endif %}

        <div class="flex-1">
          <header class="mb-4">
            <h2 class="text-2xl md:text-3xl font-semibold tracking-tight mb-3 leading-tight">
              <a href="{{ post.url }}" class="hover:text-muted-clay-dark dark:hover:text-muted-clay-light transition-colors duration-300">
                {{ post.data.title }}
              </a>
            </h2>

            <div class="blog-post-meta">
              <time datetime="{{ post.date | dateFormat('iso') }}" class="flex items-center">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
                </svg>
                {{ post.date | dateFormat }}
              </time>

              {% if post.data.tags %}
                <div class="flex flex-wrap gap-2">
                  {% for tag in post.data.tags %}
                    {% if tag != "blog" %}
                      <span class="px-2 py-1 bg-muted-light-bg-accent dark:bg-muted-bg-accent text-muted-light-text-tertiary dark:text-muted-text-tertiary text-xs rounded-full font-medium">
                        {{ tag }}
                      </span>
                    {% endif %}
                  {% endfor %}
                </div>
              {% endif %}
            </div>
          </header>

          {% if post.data.excerpt %}
          <p class="text-muted-light-text-secondary dark:text-muted-text-secondary leading-relaxed mb-4">
            {{ post.data.excerpt }}
          </p>
          {% endif %}

          <a href="{{ post.url }}" class="inline-flex items-center text-muted-stone-dark dark:text-muted-stone-light hover:text-muted-clay-dark dark:hover:text-muted-clay-light transition-colors duration-300 font-medium">
            Read more
            <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 ml-2 transform group-hover:translate-x-1 transition-transform duration-300" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7" />
            </svg>
          </a>
        </div>
      </div>
    </article>
    {%- endfor %}
  </div>

  {% if collections.blog.length === 0 %}
  <div class="text-center py-16">
    <svg xmlns="http://www.w3.org/2000/svg" class="h-16 w-16 mx-auto mb-4 text-muted-light-text-tertiary dark:text-muted-text-tertiary" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1">
      <path stroke-linecap="round" stroke-linejoin="round" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.746 0 3.332.477 4.5 1.253v13C19.832 18.477 18.246 18 16.5 18c-1.746 0-3.332.477-4.5 1.253" />
    </svg>
    <h3 class="text-xl font-semibold mb-2">No posts yet</h3>
    <p class="text-muted-light-text-secondary dark:text-muted-text-secondary">
      Check back soon for new content.
    </p>
  </div>
  {% endif %}
</div>

