---
layout: base.njk
title: Blog Posts
permalink: /blog/
---

<div class="prose max-w-none">
  <h1>{{ title }}</h1>
  <p>Browse all blog posts</p>
  
  <ul class="mt-8 space-y-6">
    {%- for post in collections.blog | reverse %}
    <li class="card">
      <h2 class="text-xl"><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
      <time datetime="{{ page.date.toISOString().split('T')[0] }}">{{ page.date.toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' }) }}</time>
    </li>
    {%- endfor %}
  </ul>
</div>

